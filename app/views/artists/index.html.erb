<h1>artists#index</h1>

<% @artist.each do |artist| %>
	<%= link_to artist do %>
		<%= image_tag artist.avatar.url(:small) %>
		<%= artist.email %>
		<%= artist.created_at %>
	<% end %>
	<br>
<% end %>
