<h1> Show Videos </h1>

<%= link_to 'Edit Video', edit_video_path %>
<%= link_to 'Delete Video', video_path, method: :delete, data: { confirm: 'Are you sure? This will permanently delete this video!' } %>

<p>
	<%= @video.title %>
	<br>
	<%= @video.description %>
	<br>
	<%= @video.rank %>
</p>